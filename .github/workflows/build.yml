name: Build AI Server

on: [push]

jobs:
  build:
    runs-on: ${{ matrix.os }}
    strategy:
      matrix:
        os: [ubuntu-latest, windows-latest]
    
    steps:
      - uses: actions/checkout@v4
      
      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.10'
          
      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          python -m pip install fastapi uvicorn pydantic torch pyinstaller
        
      - name: Build with PyInstaller
        run: |
          cd WaifuAI
          python -m PyInstaller --onefile --s --hidden-import torch --exclude-module torch.cuda --exclude-module nvidia --exclude-module tkinter --exclude-module unittest --exclude-module test say.py

      - name: Upload artifacts
        uses: actions/upload-artifact@v4
        with:
          name: server-${{ matrix.os }}
          path: WaifuAI/dist/